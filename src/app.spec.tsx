/* eslint-disable prettier/prettier */
import '@testing-library/jest-dom'

describe('Jest', () => {
  it('should work', () => {
    expect(1).toBe(1)
  })

  it('work2', () => {
    expect(1).toBe(1)
  })
})
